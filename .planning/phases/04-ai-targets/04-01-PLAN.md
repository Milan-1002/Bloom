---
phase: 04-ai-targets
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260518000002_ai_targets_insulin_score.sql
  - src/lib/database.types.ts
  - tests/ai/ai-validation.test.ts
  - src/lib/ai-validation.ts
  - supabase/functions/generate-targets/index.ts
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-05
  - AI-06

must_haves:
  truths:
    - "Calling the Edge Function with a valid user JWT and a complete profile returns a JSON object containing all 10 target fields (AI-01, AI-02, AI-03)"
    - "The Edge Function never exposes ANTHROPIC_API_KEY in any file under src/ or in any Vite env variable (AI-06)"
    - "validateTargets() throws an Error for every out-of-range value and for calorie_min >= calorie_max (AI-02)"
    - "checkForbidden() throws for each of the five medical-overreach patterns: diagnos, mg/day, prescri, Metformin, dosage (AI-05)"
    - "buildUserPrompt() produces a readable key-value string containing all six profile fields and never includes user_id (AI-01, D-01)"
    - "The ai_daily_targets table has an insulin_score smallint column with check (0..100) after migration is applied"
    - "database.types.ts contains insulin_score in the ai_daily_targets Row and Insert types after type regeneration"
  artifacts:
    - path: "supabase/migrations/20260518000002_ai_targets_insulin_score.sql"
      provides: "insulin_score smallint column + check constraint on ai_daily_targets"
      contains: "alter table public.ai_daily_targets add column insulin_score smallint"
    - path: "src/lib/database.types.ts"
      provides: "TypeScript types reflecting the new insulin_score column"
      contains: "insulin_score"
    - path: "tests/ai/ai-validation.test.ts"
      provides: "Vitest unit tests for validateTargets, checkForbidden, buildUserPrompt"
      min_lines: 60
    - path: "src/lib/ai-validation.ts"
      provides: "Pure functions: validateTargets, checkForbidden, buildUserPrompt, AITargetsOutput interface"
      exports:
        - validateTargets
        - checkForbidden
        - buildUserPrompt
        - AITargetsOutput
    - path: "supabase/functions/generate-targets/index.ts"
      provides: "Deno Edge Function: CORS, JWT auth, profile fetch, Claude call, guardrail, validation, DB insert"
      contains: "SYSTEM_PROMPT"
  key_links:
    - from: "supabase/functions/generate-targets/index.ts"
      to: "src/lib/ai-validation.ts"
      via: "functions import ai-validation pure functions (or inline duplicates for Deno compatibility)"
      pattern: "validateTargets|checkForbidden|buildUserPrompt"
    - from: "supabase/functions/generate-targets/index.ts"
      to: "public.ai_daily_targets"
      via: "supabaseUser.from('ai_daily_targets').insert({...})"
      pattern: "insulin_score.*Number"
    - from: "src/lib/ai-validation.ts"
      to: "tests/ai/ai-validation.test.ts"
      via: "Vitest import of pure functions"
      pattern: "from.*ai-validation"
---

<objective>
Deliver the complete AI targets backend for Phase 4: a Supabase Edge Function that calls Claude to
generate personalized PCOS macro targets and an Insulin Balance score, writes them to
ai_daily_targets, and enforces guardrails at both the system-prompt and regex level.

Purpose: Enables the Phase 04-02 client hook to call this function and surface real AI targets on
the dashboard. All pure business-logic functions (validateTargets, checkForbidden, buildUserPrompt)
live in src/lib/ai-validation.ts so Vitest can unit-test them outside the Deno runtime.

Output:
- supabase/migrations/20260518000002_ai_targets_insulin_score.sql — adds insulin_score column
- src/lib/database.types.ts — regenerated with insulin_score present
- tests/ai/ai-validation.test.ts — TDD RED tests (Wave 0), then GREEN after Task 4
- src/lib/ai-validation.ts — pure validation + prompt functions (Wave 1)
- supabase/functions/generate-targets/index.ts — full Deno Edge Function (Wave 1)
</objective>

<execution_context>
@C:/Users/sunar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sunar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-ai-targets/04-CONTEXT.md
@.planning/phases/04-ai-targets/04-RESEARCH.md
@.planning/phases/04-ai-targets/04-AI-SPEC.md
@.planning/phases/04-ai-targets/04-VALIDATION.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase — no exploration needed. -->

From supabase/migrations/20260518000001_init.sql (lines 161-185):
  table public.ai_daily_targets columns:
    id uuid, user_id uuid, generated_at timestamptz, protein_g numeric(6,2),
    fiber_g numeric(6,2), gl_target numeric(6,2), added_sugar_g numeric(6,2),
    calorie_min integer, calorie_max integer, pc_ratio_target numeric(4,2),
    insulin_score_basis jsonb, narrative text, prompt_version integer default 1,
    created_at timestamptz
  RLS policies:
    ai_targets_select_own: for select using ((select auth.uid()) = user_id)
    ai_targets_insert_own: for insert with check ((select auth.uid()) = user_id)
  NOTE: insulin_score column does NOT yet exist — Task 1 adds it.

From supabase/functions/delete-account/index.ts (JWT auth boilerplate):
  createClient(SUPABASE_URL, SUPABASE_ANON_KEY, { global: { headers: { Authorization: authHeader } } })
  supabaseUser.auth.getUser() → { data: { user }, error }
  Pattern: no CORS headers in existing functions — generate-targets MUST add them (see RESEARCH.md Pitfall 1)

From src/lib/targets.ts:
  STATIC_TARGETS = { protein_g: 120, fiber_g: 30, gl: 100, sugar_g: 25, kcal: 1700 }
  DailyTargets = typeof STATIC_TARGETS
  Phase 04-02 will extend this with AITargets type (out of scope here)

AITargetsOutput interface (to be created in src/lib/ai-validation.ts):
  protein_g: number        // assertRange [40, 300]
  fiber_g: number          // assertRange [15, 80]
  gl_target: number        // assertRange [40, 200]
  added_sugar_g: number    // assertRange [10, 60]
  calorie_min: number      // assertRange [1200, 2500]
  calorie_max: number      // assertRange [1400, 3000]
  pc_ratio_target: number  // assertRange [0.15, 0.60]
  insulin_score: number    // assertRange [0, 100]
  narrative: string        // non-empty, <= 300 chars
  key_factors: string[]    // non-empty array
  cross-field: calorie_min < calorie_max, calorie_max - calorie_min >= 200

FORBIDDEN_PATTERNS (to be created in src/lib/ai-validation.ts):
  /diagnos/i, /mg\/day/i, /prescri/i, /[Mm]etformin/, /dosage/i
</interfaces>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════════════ -->
<!-- WAVE 0 — Prerequisites (must complete before Wave 1)                   -->
<!-- ═══════════════════════════════════════════════════════════════════════ -->

<task type="auto" wave="0">
  <name>Task 1: Write migration 20260518000002_ai_targets_insulin_score.sql</name>
  <files>supabase/migrations/20260518000002_ai_targets_insulin_score.sql</files>
  <read_first>
    - supabase/migrations/20260518000001_init.sql lines 160-186 (existing ai_daily_targets schema —
      confirm column names before writing ALTER TABLE; insulin_score_basis jsonb already exists)
    - .planning/phases/04-ai-targets/04-RESEARCH.md "Migration SQL" section (confirmed SQL + rationale
      for nullable without DEFAULT)
    - .planning/phases/04-ai-targets/04-CONTEXT.md D-08 (smallint, 0-100 check, queryable integer)
  </read_first>
  <action>
    Create the migration file with a single ALTER TABLE statement. The file must:

    - Add column insulin_score smallint to public.ai_daily_targets with check constraint
      (insulin_score >= 0 and insulin_score <= 100). Do NOT add NOT NULL or DEFAULT — existing rows
      must not be assigned a score (per D-08 and RESEARCH.md nullable rationale).
    - Add a COMMENT ON COLUMN describing the 0-40/41-70/71-100 interpretation bands (per D-09).
    - File header comment must cite: "Phase 4-01: adds insulin_score smallint column separate from
      insulin_score_basis jsonb which stores { key_factors: string[] }".
    - No other changes — do not touch RLS policies, indexes, or other tables.
    - Filename exactly: 20260518000002_ai_targets_insulin_score.sql
  </action>
  <verify>
    <automated>grep -q "insulin_score smallint" supabase/migrations/20260518000002_ai_targets_insulin_score.sql && grep -q "check (insulin_score >= 0 and insulin_score <= 100)" supabase/migrations/20260518000002_ai_targets_insulin_score.sql && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists at supabase/migrations/20260518000002_ai_targets_insulin_score.sql
    - Contains "alter table public.ai_daily_targets" (exact casing)
    - Contains "add column insulin_score smallint"
    - Contains check constraint for range 0-100
    - Does NOT contain "NOT NULL" or "DEFAULT" on the insulin_score line
    - COMMENT ON COLUMN present with score interpretation text
  </acceptance_criteria>
  <done>Migration SQL file written and passes grep assertions above.</done>
</task>

<task type="auto" wave="0" gate="BLOCKING">
  <name>Task 2 [BLOCKING]: Apply migration and regenerate TypeScript types</name>
  <files>src/lib/database.types.ts</files>
  <read_first>
    - supabase/migrations/20260518000002_ai_targets_insulin_score.sql (just created in Task 1 —
      must exist before this task runs)
    - src/lib/database.types.ts (current state — confirm ai_daily_targets Row type lacks insulin_score
      before running regen; confirm it appears after)
    - .planning/phases/04-ai-targets/04-RESEARCH.md "Pitfall 2" section (TypeScript error if types
      not regenerated before Edge Function references the new column)
  </read_first>
  <action>
    This task has two sequential steps — both must succeed before proceeding to Wave 1.

    Step A — Apply the migration to the remote Supabase project:
      Command: supabase db push
      Expected: Migration 20260518000002_ai_targets_insulin_score.sql is applied. If supabase CLI
      requires interactive confirmation (e.g., "Apply migrations to remote?"), the executor must
      respond "yes". If the CLI is not authenticated, run: supabase login first.
      Alternative if CLI unavailable: Apply SQL directly via Supabase dashboard SQL editor using
      the exact SQL from the migration file.

    Step B — Regenerate TypeScript types:
      Command: supabase gen types typescript --project-id nfyqateokdecjttimrhy > src/lib/database.types.ts
      This overwrites src/lib/database.types.ts with types reflecting the new column.
      The ai_daily_targets.Row type must gain: insulin_score: number | null
      The ai_daily_targets.Insert type must gain: insulin_score?: number | null

    Do NOT proceed to Task 3 until grep on src/lib/database.types.ts confirms insulin_score is present.
    The ANTHROPIC_API_KEY secret registration is a separate manual step noted in the threat model —
    it is NOT part of this automated task.
  </action>
  <verify>
    <automated>grep -q "insulin_score" src/lib/database.types.ts && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - supabase db push exits 0 (or SQL applied via dashboard)
    - src/lib/database.types.ts contains the string "insulin_score" (at minimum once, inside the
      ai_daily_targets table type block)
    - No TypeScript compile errors in src/lib/database.types.ts (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>Migration applied to remote DB. database.types.ts regenerated and contains insulin_score.</done>
</task>

<task type="tdd" wave="0">
  <name>Task 3 (TDD RED): Write failing tests for validateTargets, checkForbidden, buildUserPrompt</name>
  <files>tests/ai/ai-validation.test.ts</files>
  <read_first>
    - .planning/phases/04-ai-targets/04-RESEARCH.md "Validation Architecture" section (test map,
      Vitest framework, test file path, quick run command)
    - .planning/phases/04-ai-targets/04-RESEARCH.md Pattern 5 (validateTargets interface and ranges)
    - .planning/phases/04-ai-targets/04-RESEARCH.md Pattern 6 (FORBIDDEN_PATTERNS list)
    - .planning/phases/04-ai-targets/04-RESEARCH.md Pattern 4 (buildUserPrompt behavior)
    - tests/ directory (read any existing test file to confirm import style and vi.mock patterns)
    - vitest.config.ts (confirm path aliases — @/ resolves to src/)
  </read_first>
  <action>
    Create tests/ai/ai-validation.test.ts with Vitest tests for three pure functions imported from
    src/lib/ai-validation.ts. At TDD RED phase, src/lib/ai-validation.ts does not yet exist, so
    ALL imports will fail — this is the expected RED state.

    Test groups to write:

    GROUP 1 — validateTargets():
      - happy path: valid AITargetsOutput object passes and is returned unchanged
      - protein_g below floor (39): throws with message containing "protein_g"
      - protein_g above ceiling (301): throws
      - calorie_min >= calorie_max (1800 >= 1800): throws
      - calorie range too narrow (calorie_max - calorie_min < 200): throws
      - narrative empty string: throws
      - narrative over 300 chars: throws (use a string of 301 'x' chars)
      - key_factors empty array: throws
      - not an object (null): throws
      - insulin_score = 101: throws (out of range)
      - fiber_g = 14: throws (below 15 floor)

    GROUP 2 — checkForbidden():
      - "diagnoses insulin resistance" → throws (pattern: /diagnos/i)
      - "take 500mg/day of inositol" → throws (pattern: /mg\/day/i)
      - "we prescribe this for" → throws (pattern: /prescri/i)
      - "Metformin interactions" → throws (pattern: /[Mm]etformin/)
      - "dosage adjustment needed" → throws (pattern: /dosage/i)
      - "Your targets support steady energy" → does NOT throw

    GROUP 3 — buildUserPrompt():
      - output contains "PCOS type: Insulin Resistance" when profile.pcos_type = "Insulin Resistance"
      - output contains "Age: 32" when profile.age = 32
      - output contains "Height: 165 cm" when profile.height_cm = 165
      - output contains "weight loss, steady energy" when profile.goals = ["weight loss", "steady energy"]
      - output does NOT contain the user_id value (GDPR: user_id must never enter the prompt)
      - output contains "Generate daily macro targets" at the end
      - pcos_type = null → output contains "not specified"

    Use describe/it blocks. Import style:
      import { validateTargets, checkForbidden, buildUserPrompt } from '@/lib/ai-validation'
      import type { AITargetsOutput } from '@/lib/ai-validation'

    TDD RED GATE: After writing the file, run the test suite. ALL tests must FAIL (import error or
    missing export). Do NOT create src/lib/ai-validation.ts yet — that is Task 4.
    Commit message: "test(04-01): RED — ai-validation failing tests for validateTargets, checkForbidden, buildUserPrompt"
  </action>
  <verify>
    <automated>npx vitest run tests/ai/ai-validation.test.ts 2>&1 | grep -E "FAIL|failed" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/ai/ai-validation.test.ts
    - File contains describe blocks for validateTargets, checkForbidden, and buildUserPrompt
    - Running npx vitest run tests/ai/ai-validation.test.ts exits non-zero (tests FAIL = RED state confirmed)
    - Failure reason is import error or missing export — NOT a syntax error in the test file itself
    - src/lib/ai-validation.ts does NOT exist after this task
  </acceptance_criteria>
  <done>Test file written with all three groups. Suite is RED (all failing due to missing module). Committed as RED.</done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════════ -->
<!-- WAVE 1 — Implementation                                                 -->
<!-- ═══════════════════════════════════════════════════════════════════════ -->

<task type="auto" tdd="true" wave="1">
  <name>Task 4 (TDD GREEN): Write src/lib/ai-validation.ts — pure functions</name>
  <files>src/lib/ai-validation.ts</files>
  <read_first>
    - tests/ai/ai-validation.test.ts (just created in Task 3 — implement against these exact tests)
    - .planning/phases/04-ai-targets/04-RESEARCH.md Pattern 5 (validateTargets full implementation
      including assertRange helper and cross-field checks — calorie_min >= calorie_max, range < 200)
    - .planning/phases/04-ai-targets/04-RESEARCH.md Pattern 6 (checkForbidden — FORBIDDEN_PATTERNS array)
    - .planning/phases/04-ai-targets/04-RESEARCH.md Pattern 4 (buildUserPrompt — profile field
      serialization, "not specified" fallback, user_id excluded per GDPR)
    - .planning/phases/04-ai-targets/04-AI-SPEC.md Section 4b (AITargetsOutput interface — canonical
      field list and ranges)
  </read_first>
  <behavior>
    - validateTargets(valid_object) → returns same object typed as AITargetsOutput
    - validateTargets({protein_g: 39, ...}) → throws Error containing "protein_g"
    - validateTargets({calorie_min: 1800, calorie_max: 1800, ...}) → throws Error containing "calorie_min"
    - validateTargets({calorie_min: 1400, calorie_max: 1550, ...}) → throws (range < 200)
    - validateTargets({narrative: "", ...}) → throws
    - validateTargets({narrative: "x".repeat(301), ...}) → throws
    - validateTargets({key_factors: [], ...}) → throws
    - validateTargets(null) → throws
    - checkForbidden("diagnoses this condition") → throws
    - checkForbidden("Your targets support steady energy") → does not throw
    - buildUserPrompt({pcos_type: "IR", age: 32, ...}) → string containing "PCOS type: IR" and "Age: 32"
    - buildUserPrompt({...profile, user_id: "abc-123"}) → string NOT containing "abc-123"
  </behavior>
  <action>
    Create src/lib/ai-validation.ts as a Node.js-compatible TypeScript module (no Deno globals —
    this file must import cleanly in Vitest/jsdom without any Deno.* references).

    Export the following from this file:

    1. AITargetsOutput interface — 10 fields with their TypeScript types (number, string, string[]).
       Match the exact field names in D-03: protein_g, fiber_g, gl_target, added_sugar_g,
       calorie_min, calorie_max, pc_ratio_target, insulin_score, narrative, key_factors.

    2. assertRange(name: string, val: number, min: number, max: number): void — throws Error with
       descriptive message on out-of-range or non-finite values. Not exported from module (internal
       helper), but used by validateTargets.

    3. validateTargets(raw: unknown): AITargetsOutput — validates all 10 fields plus two cross-field
       checks (calorie_min < calorie_max; calorie_max - calorie_min >= 200). Throws on any violation.

    4. FORBIDDEN_PATTERNS: RegExp[] — array of 5 patterns:
       /diagnos/i, /mg\/day/i, /prescri/i, /[Mm]etformin/, /dosage/i
       Export this constant so tests can reference it for exhaustiveness checks.

    5. checkForbidden(text: string): void — iterates FORBIDDEN_PATTERNS, throws on first match with
       message "forbidden:${pattern.source}".

    6. ProfileInput type — shape of the profile object for buildUserPrompt:
       { pcos_type: string | null, goals: string[] | null, age: number | null,
         height_cm: number | null, current_weight_kg: number | null, goal_weight_kg: number | null }
       Do NOT include user_id — this enforces the GDPR constraint at the type level (per D-01).

    7. buildUserPrompt(profile: ProfileInput): string — serializes profile as readable key-value
       pairs (one per line), falls back to "not specified" for null fields, appends
       "\n\nGenerate daily macro targets for this user." at the end.

    TDD GREEN GATE: After writing, run npx vitest run tests/ai/ai-validation.test.ts — all tests
    must PASS. Fix any failures before proceeding to Task 5.
    Commit message: "feat(04-01): GREEN — ai-validation pure functions"
  </action>
  <verify>
    <automated>npx vitest run tests/ai/ai-validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run tests/ai/ai-validation.test.ts exits 0 (all tests GREEN)
    - File exports: AITargetsOutput, validateTargets, FORBIDDEN_PATTERNS, checkForbidden, ProfileInput, buildUserPrompt
    - File contains zero references to Deno (no Deno.env, Deno.serve, etc.)
    - npx tsc --noEmit exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>All ai-validation tests green. Module exported cleanly. No Deno references. TypeScript clean.</done>
</task>

<task type="auto" wave="1">
  <name>Task 5: Write supabase/functions/generate-targets/index.ts — full Edge Function</name>
  <files>supabase/functions/generate-targets/index.ts</files>
  <read_first>
    - supabase/functions/delete-account/index.ts (copy JWT verification block verbatim — lines 1-18;
      createClient with user JWT pattern; auth.getUser() pattern)
    - supabase/functions/usda-search/index.ts (error response shape — JSON.stringify + status code;
      Deno.env.get pattern for secrets)
    - .planning/phases/04-ai-targets/04-RESEARCH.md "Complete Edge Function Structure" section
      (full reference implementation — use this as the implementation blueprint)
    - .planning/phases/04-ai-targets/04-RESEARCH.md "System Prompt Design" section (full SYSTEM_PROMPT
      text — copy verbatim including STRICT PROHIBITIONS, FRAMING RULES, OUTPUT FORMAT, EXAMPLE OUTPUT,
      BIOLOGICAL CONSTRAINTS sections)
    - src/lib/ai-validation.ts (just created in Task 4 — the pure functions exist there, but this Deno
      file CANNOT import from src/; inline validateTargets, checkForbidden, buildUserPrompt, and
      FORBIDDEN_PATTERNS directly in this file to avoid cross-runtime import issues)
    - src/lib/database.types.ts (confirm insulin_score column is now in TablesInsert<'ai_daily_targets'>)
    - .planning/phases/04-ai-targets/04-CONTEXT.md D-13 through D-18 (response shape, user JWT,
      error codes, HTTP status codes)
    - .planning/phases/04-ai-targets/04-RESEARCH.md "Anti-Patterns to Avoid" (service role prohibition,
      JSON.parse without try/catch, temperature != 0, user_id in prompt)
    - .planning/phases/04-ai-targets/04-RESEARCH.md Pitfall 3 (calorie_min >= calorie_max cross-field check)
  </read_first>
  <action>
    Create supabase/functions/generate-targets/index.ts. This is a Deno TypeScript file — it uses
    Deno.serve, Deno.env, and imports from esm.sh CDN. It CANNOT import from src/.

    Structure of the file (in order):

    1. Imports (two CDN imports, no npm: specifier):
       import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
       import Anthropic from 'https://esm.sh/@anthropic-ai/sdk'

    2. Constants:
       const PROMPT_VERSION = 1
       const corsHeaders object with Access-Control-Allow-Origin and Access-Control-Allow-Headers

    3. SYSTEM_PROMPT string constant — copy the full system prompt from RESEARCH.md "System Prompt
       Design" section verbatim. This is approximately 450 tokens and includes:
       - Persona: "You are a PCOS nutrition specialist AI..."
       - STRICT PROHIBITIONS block (per D-05, AI-05)
       - FRAMING RULES block (per D-06)
       - OUTPUT FORMAT block with exact 10-field JSON schema (per D-03)
       - EXAMPLE OUTPUT block with confirmed PCOS/insulin-resistance/weight-loss values
       - BIOLOGICAL CONSTRAINTS block (protein >= 90 for weight loss, calorie_min never < 1200, etc.)

    4. Inline the pure functions from src/lib/ai-validation.ts (do NOT import from src/):
       - AITargetsOutput interface (same field names and types)
       - ProfileRow type (pcos_type, goals, age, height_cm, current_weight_kg, goal_weight_kg —
         no user_id per GDPR / D-01)
       - assertRange helper function
       - validateTargets function (including cross-field calorie checks from RESEARCH.md Pitfall 3)
       - FORBIDDEN_PATTERNS array (5 patterns)
       - checkForbidden function
       - buildUserPrompt function

    5. Anthropic client initialization (module-level, outside Deno.serve):
       const anthropic = new Anthropic({ apiKey: Deno.env.get('ANTHROPIC_API_KEY')! })

    6. Deno.serve handler — implement the 11-step flow from RESEARCH.md System Architecture Diagram:
       a. OPTIONS preflight → return Response('ok', { headers: corsHeaders })
       b. Define jsonHeaders = { ...corsHeaders, 'Content-Type': 'application/json' }
       c. Wrap all logic in try/catch; catch returns HTTP 500 with { error: 'generation_failed', message }
       d. Get Authorization header → 401 if missing (per D-13)
       e. createClient with user JWT (SUPABASE_URL, SUPABASE_ANON_KEY, global.headers.Authorization)
       f. supabaseUser.auth.getUser() → 401 if error or no user
       g. supabaseUser.from('profiles').select('pcos_type, goals, age, height_cm, current_weight_kg,
          goal_weight_kg').eq('id', user.id).single()
          → 400 { error: 'profile_required' } if error, no row, or profile.pcos_type is null (per D-18)
       h. anthropic.messages.create({ model: 'claude-sonnet-4-6', max_tokens: 512, temperature: 0,
          system: SYSTEM_PROMPT, messages: [{ role: 'user', content: buildUserPrompt(profile) }] })
       i. Extract text: message.content[0].type === 'text' ? message.content[0].text : ''
          throw if text is empty
       j. checkForbidden(text) — throw if forbidden pattern matched (per AI-05)
       k. JSON.parse(text) in try/catch — throw on parse failure (per D-02)
       l. validateTargets(parsed) — throw on invalid ranges or cross-field violations
       m. const generated_at = new Date().toISOString()
       n. supabaseUser.from('ai_daily_targets').insert({
            user_id: user.id,
            protein_g, fiber_g, gl_target, added_sugar_g, calorie_min, calorie_max,
            pc_ratio_target, insulin_score, insulin_score_basis: { key_factors: targets.key_factors },
            narrative, prompt_version: PROMPT_VERSION, generated_at
          }) — throw if insertError
       o. Return JSON response (per D-13 response shape): { protein_g, fiber_g, gl_target,
          added_sugar_g, calorie_min, calorie_max, pc_ratio_target, insulin_score, narrative,
          generated_at } — note: key_factors NOT in response, generated_at IS in response

    Error logging: On catch, log only error_type and message truncated to 200 chars via console.error.
    Do NOT log full profile data or full Claude response (GDPR / AI-SPEC.md Section 1b).

    ANTHROPIC_API_KEY registration note: Add a comment at the top of the file:
    "// Requires ANTHROPIC_API_KEY secret: supabase secrets set ANTHROPIC_API_KEY=<key>"
    This is the manual secret registration step for the user — not automated by this task.
  </action>
  <verify>
    <automated>grep -q "ANTHROPIC_API_KEY" supabase/functions/generate-targets/index.ts && grep -q "SYSTEM_PROMPT" supabase/functions/generate-targets/index.ts && grep -q "checkForbidden" supabase/functions/generate-targets/index.ts && grep -q "validateTargets" supabase/functions/generate-targets/index.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists at supabase/functions/generate-targets/index.ts
    - Contains SYSTEM_PROMPT constant with STRICT PROHIBITIONS, OUTPUT FORMAT, and EXAMPLE OUTPUT blocks
    - Contains PROMPT_VERSION = 1
    - Contains corsHeaders object with Access-Control-Allow-Origin
    - CORS OPTIONS handler returns 200 before any JWT logic
    - anthropic.messages.create call uses model 'claude-sonnet-4-6', temperature: 0, max_tokens: 512
    - checkForbidden called on raw text BEFORE JSON.parse
    - validateTargets called AFTER JSON.parse
    - DB insert uses supabaseUser (user JWT) NOT a service role client
    - DB insert includes insulin_score as a top-level column (not only inside insulin_score_basis)
    - Response includes generated_at; does NOT include key_factors or insulin_score_basis
    - No references to "ANTHROPIC_API_KEY" anywhere in src/ directory
    - grep -r "ANTHROPIC_API_KEY" src/ exits non-zero (key not in client bundle)
    - npx vitest run exits 0 (full test suite green after this task)
  </acceptance_criteria>
  <done>Edge Function written. Grep assertions pass. ANTHROPIC_API_KEY not in src/. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Edge Function | Browser sends JWT in Authorization header; function must verify signature server-side before any DB access |
| Edge Function → Anthropic API | API key must live only in Deno.env; never in response body, logs, or Vite build |
| Edge Function → Supabase DB | User JWT scopes all DB operations; RLS enforces row ownership |
| Profile data → Claude prompt | Health-adjacent PII (age, weight, PCOS type) enters Anthropic's API; user_id must never appear in prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Spoofing | generate-targets/index.ts: auth.getUser() | mitigate | supabaseUser.auth.getUser() with user-scoped client validates JWT signature and expiry; return 401 on error or missing user |
| T-04-01-02 | Tampering | generate-targets/index.ts: Claude output → DB insert | mitigate | validateTargets() throws on out-of-range values; cross-field calorie check; checkForbidden() blocks medical content before JSON.parse |
| T-04-01-03 | Information Disclosure | ANTHROPIC_API_KEY | mitigate | Deno.env.get('ANTHROPIC_API_KEY') in Edge Function only; never in .env.local, .env, or any src/ file; verified by grep in Task 5 acceptance criteria |
| T-04-01-04 | Information Disclosure | PII in logs | mitigate | console.error logs only error_type + message[:200]; never logs full Claude prompt, profile fields, or user.id (per AI-SPEC.md Section 1b) |
| T-04-01-05 | Elevation of Privilege | ai_daily_targets DB insert | mitigate | User JWT client used for insert; RLS INSERT policy (with check auth.uid() = user_id) prevents cross-user writes; service role NOT used for this operation |
| T-04-01-06 | Information Disclosure | Cross-user ai_daily_targets SELECT | mitigate | RLS SELECT policy limits reads to own rows; user JWT scopes supabaseUser client |
| T-04-01-07 | Repudiation | Medical overreach by Claude | mitigate | SYSTEM_PROMPT STRICT PROHIBITIONS block (per D-05); checkForbidden() regex guardrail on 5 patterns before DB insert; HTTP 500 on trigger |
| T-04-01-08 | Denial of Service | Claude API unavailability | accept | HTTP 500 returned to client; Phase 04-02 hook catches 5xx and falls back to STATIC_TARGETS; acceptable for v1 volume |
| T-04-01-SC | Tampering | esm.sh CDN imports | mitigate | Both CDN packages are official vendor SDKs (@supabase/supabase-js@2 pinned version; @anthropic-ai/sdk from anthropic.com); Package Legitimacy Audit in RESEARCH.md confirms [OK] for both |
</threat_model>

<verification>
After all tasks complete, run the following checks:

1. Full Vitest suite passes:
   npx vitest run

2. ai-validation module exports confirmed:
   grep -E "export (function|const|interface|type)" src/lib/ai-validation.ts

3. ANTHROPIC_API_KEY not in client bundle:
   grep -r "ANTHROPIC_API_KEY" src/ && echo FAIL || echo PASS

4. insulin_score in regenerated types:
   grep "insulin_score" src/lib/database.types.ts

5. Migration file exists with correct constraint:
   grep "check (insulin_score >= 0 and insulin_score <= 100)" supabase/migrations/20260518000002_ai_targets_insulin_score.sql

6. Edge Function key structural checks:
   grep -q "PROMPT_VERSION = 1" supabase/functions/generate-targets/index.ts
   grep -q "temperature: 0" supabase/functions/generate-targets/index.ts
   grep -q "checkForbidden" supabase/functions/generate-targets/index.ts

7. Manual smoke test (requires local Supabase stack + ANTHROPIC_API_KEY secret):
   supabase functions serve generate-targets
   curl -X POST http://localhost:54321/functions/v1/generate-targets \
     -H "Authorization: Bearer <valid_user_jwt>" \
     -H "Content-Type: application/json"
   Expect: HTTP 200 with JSON containing protein_g, fiber_g, gl_target, added_sugar_g,
           calorie_min, calorie_max, pc_ratio_target, insulin_score, narrative, generated_at
</verification>

<success_criteria>
Phase 04-01 is complete when ALL of the following are true:

1. supabase/migrations/20260518000002_ai_targets_insulin_score.sql exists with the correct ALTER TABLE
   and check constraint for insulin_score smallint (0..100)

2. src/lib/database.types.ts contains "insulin_score" (applied migration + type regen confirmed)

3. npx vitest run exits 0 — all tests green including tests/ai/ai-validation.test.ts covering
   validateTargets (11 cases), checkForbidden (6 cases), buildUserPrompt (7 cases)

4. src/lib/ai-validation.ts exports AITargetsOutput, validateTargets, FORBIDDEN_PATTERNS,
   checkForbidden, ProfileInput, buildUserPrompt — zero Deno references

5. supabase/functions/generate-targets/index.ts exists with SYSTEM_PROMPT, PROMPT_VERSION,
   corsHeaders, full 11-step handler, and inlined pure functions

6. grep -r "ANTHROPIC_API_KEY" src/ exits non-zero (key not present in client bundle)

7. ANTHROPIC_API_KEY secret registered via: supabase secrets set ANTHROPIC_API_KEY=<key>
   (confirmed via: supabase secrets list | grep ANTHROPIC_API_KEY)
</success_criteria>

<output>
Create .planning/phases/04-ai-targets/04-01-SUMMARY.md when done.
</output>
