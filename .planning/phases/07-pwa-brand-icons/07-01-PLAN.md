---
phase: 07-pwa-brand-icons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/icon-source.svg
  - public/pwa-64x64.png
  - public/pwa-72x72.png
  - public/pwa-96x96.png
  - public/pwa-128x128.png
  - public/pwa-144x144.png
  - public/pwa-152x152.png
  - public/pwa-192x192.png
  - public/pwa-384x384.png
  - public/pwa-512x512.png
  - public/maskable-icon-512x512.png
  - public/apple-touch-icon-180x180.png
  - public/favicon.ico
  - scripts/generate-icons.mjs
  - vite.config.ts
  - index.html
autonomous: true
requirements:
  - PWA-01

must_haves:
  truths:
    - "When a user installs the Bloom PWA, the home screen icon shows the Bloom lightning-bolt mark on a slate navy (#1F3A4D) background — not a blank or broken square"
    - "The installed app icon renders without pixelation at 72, 96, 128, 144, 152, 192, 384, and 512px"
    - "The browser tab favicon shows the Bloom mark (existing favicon.svg retained for browser use)"
    - "Chrome's PWA installability checker passes without icon-related warnings (192px and 512px present, maskable separate entry)"
    - "iOS home screen shows the correct icon via apple-touch-icon-180x180.png"
    - "The app tab and install dialog display 'Bloom — PCOS Nutrition', not 'app-tmp'"
  artifacts:
    - path: "public/icon-source.svg"
      provides: "Filter-free flat SVG source — 512×512 viewBox, solid #1F3A4D background, white lightning-bolt mark in inner 80% safe zone, no <filter> or <feGaussianBlur> elements"
    - path: "public/pwa-192x192.png"
      provides: "Real brand mark PNG (replaces 69-byte 1×1px placeholder)"
    - path: "public/pwa-512x512.png"
      provides: "Real brand mark PNG (replaces 69-byte 1×1px placeholder)"
    - path: "public/maskable-icon-512x512.png"
      provides: "Separate maskable variant — mark confined to inner 80%, full-bleed #1F3A4D background"
    - path: "public/apple-touch-icon-180x180.png"
      provides: "iOS home screen icon at 180×180px"
    - path: "scripts/generate-icons.mjs"
      provides: "Repeatable script for extra sizes (72, 96, 128, 144, 152, 384px) using sharp"
    - path: "vite.config.ts"
      provides: "Updated manifest.icons with 10 entries using correct purpose split and /pwa-*.png root paths"
    - path: "index.html"
      provides: "apple-touch-icon link, theme-color meta, correct title 'Bloom — PCOS Nutrition'"
  key_links:
    - from: "vite.config.ts manifest.icons"
      to: "public/pwa-192x192.png and public/pwa-512x512.png"
      via: "src paths must match /pwa-*.png (root), not /icons/pwa-*.png"
      pattern: "src: '/pwa-192x192\\.png'"
    - from: "index.html"
      to: "public/apple-touch-icon-180x180.png"
      via: "<link rel='apple-touch-icon' href='/apple-touch-icon-180x180.png'>"
      pattern: "apple-touch-icon"
    - from: "public/icon-source.svg"
      to: "all public/pwa-*.png files"
      via: "@vite-pwa/assets-generator CLI + scripts/generate-icons.mjs"
      pattern: "icon-source\\.svg"
---

<objective>
Deliver real Bloom brand icons for the PWA — replacing the two 69-byte 1×1px transparent placeholder PNGs that currently ship with the app.

Purpose: Users who install the Bloom PWA see the Bloom lightning-bolt mark on their home screen instead of a blank or broken icon. The app passes Chrome's PWA installability checklist without icon warnings.

Output:
- public/icon-source.svg — filter-free flat Bloom mark SVG (the rasterization master)
- public/pwa-64x64.png through pwa-512x512.png — all 9 sizes at public root
- public/maskable-icon-512x512.png — separate maskable variant
- public/apple-touch-icon-180x180.png — iOS home screen icon
- public/favicon.ico — multi-resolution ICO for legacy browsers
- scripts/generate-icons.mjs — repeatable script for the 6 extra sizes sharp produces
- vite.config.ts updated — 10 correct manifest icon entries, no 'any maskable' anti-pattern
- index.html updated — apple-touch-icon link, theme-color, correct title
</objective>

<execution_context>
@C:\Users\sunar\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\sunar\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pwa-brand-icons/07-RESEARCH.md

<interfaces>
<!-- Current vite.config.ts manifest.icons (anti-pattern — to be replaced): -->
icons: [
  { src: '/icons/pwa-192x192.png', sizes: '192x192', type: 'image/png' },
  { src: '/icons/pwa-512x512.png', sizes: '512x512', type: 'image/png', purpose: 'any maskable' },
]

<!-- Current index.html head (to be updated): -->
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>app-tmp</title>

<!-- favicon.svg lightning-bolt path (source for icon-source.svg recreation — filter-free): -->
<!-- Original viewBox="0 0 48 46" path (the fill shape, no filters): -->
<!-- M25.946 44.938c-.664.845-2.021.375-2.021-.698V33.937a2.26 2.26 0 0 0-2.262-2.262H10.287c-.92 0-1.456-1.04-.92-1.788l7.48-10.471c1.07-1.497 0-3.578-1.842-3.578H1.237c-.92 0-1.456-1.04-.92-1.788L10.013.474c.214-.297.556-.474.92-.474h28.894c.92 0 1.456 1.04.92 1.788l-7.48 10.471c-1.07 1.498 0 3.579 1.842 3.579h11.377c.943 0 1.473 1.088.89 1.83L25.947 44.94z -->
<!-- This path is on a 48×46 canvas; scale with transform="translate(56, 40) scale(8.33)" to center in 512×512 -->

<!-- @vite-pwa/assets-generator minimal-2023 preset output files (all land in same dir as source SVG): -->
<!-- pwa-64x64.png, pwa-192x192.png, pwa-512x512.png, maskable-icon-512x512.png, apple-touch-icon-180x180.png, favicon.ico -->

<!-- sharp API (used in scripts/generate-icons.mjs for extra sizes): -->
<!-- import sharp from 'sharp' -->
<!-- sharp(svgBuffer).resize(size, size).png().toFile(outputPath) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author icon-source.svg, install deps, generate all PNG assets</name>
  <files>
    public/icon-source.svg
    scripts/generate-icons.mjs
    public/pwa-64x64.png
    public/pwa-72x72.png
    public/pwa-96x96.png
    public/pwa-128x128.png
    public/pwa-144x144.png
    public/pwa-152x152.png
    public/pwa-192x192.png
    public/pwa-384x384.png
    public/pwa-512x512.png
    public/maskable-icon-512x512.png
    public/apple-touch-icon-180x180.png
    public/favicon.ico
  </files>
  <read_first>
    - public/favicon.svg — extract the lightning-bolt path data (the single `<path fill="#863bff" d="M25.946...">` element on a 48×46 viewBox); this is the Bloom mark to recreate flat
    - .planning/phases/07-pwa-brand-icons/07-RESEARCH.md — Pattern 1 (SVG authoring constraints), Pattern 2 (CLI command), Pattern 3 (extra sizes script), safe zone math
  </read_first>
  <action>
    Step 1 — Install @vite-pwa/assets-generator as a devDependency:
    `npm install -D @vite-pwa/assets-generator`
    (sharp installs transitively; do not install sharp directly)

    Step 2 — Create public/icon-source.svg with these EXACT constraints:
    - viewBox="0 0 512 512" width="512" height="512"
    - No `<filter>`, `<feGaussianBlur>`, `<feMerge>`, or `<mask>` elements — they break sharp/librsvg rasterization
    - Full-bleed solid background: `<rect width="512" height="512" fill="#1F3A4D"/>` (slate navy per user decision)
    - All fill attributes use literal hex values — NOT `var(--b-primary)` (CSS variables do not resolve during SVG rasterization)
    - White mark fill: `#FFFFFF`
    - The Bloom lightning-bolt mark from favicon.svg is on a 48×46 viewBox. To center it in the safe zone (inner 80% = 410×410px region starting at offset 51,51) apply a transform group:
      `<g transform="translate(256, 262) scale(8.33) translate(-24, -23)">` — this maps the 48×46 path to ~400px centered at canvas center 256,256. Adjust the Y translate if the mark appears too high or low — the path goes from y=0 to y=44.938, center is ~22.5, so translate(-24, -22.5) is accurate.
    - Copy the lightning-bolt path `d` attribute verbatim from favicon.svg `<path fill="#863bff" d="M25.946 44.938...">` into the icon-source.svg, changing fill to `#FFFFFF`
    - The icon-source.svg is the ONLY file to be rasterized — the existing favicon.svg stays untouched (it renders fine in browsers; the filter problem only affects sharp)

    Step 3 — Run the @vite-pwa/assets-generator CLI to generate the minimal-2023 preset files (pwa-64x64.png, pwa-192x192.png, pwa-512x512.png, maskable-icon-512x512.png, apple-touch-icon-180x180.png, favicon.ico), all output to public/:
    `npx @vite-pwa/assets-generator --preset minimal-2023 public/icon-source.svg`
    The tool writes output to the same directory as the source file (public/).

    Step 4 — Create scripts/generate-icons.mjs to produce the 6 extra sizes (72, 96, 128, 144, 152, 384px) that minimal-2023 does not generate:
    - ESM module (package.json has "type": "module")
    - Import sharp from 'sharp', readFileSync from 'fs', join/dirname from 'path', fileURLToPath from 'url'
    - Derive __dirname using `dirname(fileURLToPath(import.meta.url))`
    - Read source: `readFileSync(join(__dirname, '../public/icon-source.svg'))`
    - Loop over sizes [72, 96, 128, 144, 152, 384], for each: `await sharp(svgBuffer).resize(size, size).png().toFile(join(__dirname, '../public/pwa-' + size + 'x' + size + '.png'))`
    - Log each generated file name

    Step 5 — Run the script:
    `node scripts/generate-icons.mjs`

    Step 6 — Verify output: all 14 PNG files now exist in public/:
    pwa-64x64.png, pwa-72x72.png, pwa-96x96.png, pwa-128x128.png, pwa-144x144.png,
    pwa-152x152.png, pwa-192x192.png, pwa-384x384.png, pwa-512x512.png,
    maskable-icon-512x512.png, apple-touch-icon-180x180.png, favicon.ico

    Do NOT delete public/icons/ yet — that is handled in Task 2 after vite.config.ts is updated.
    Do NOT modify vite.config.ts or index.html in this task.
  </action>
  <verify>
    <automated>
      node -e "
      const { existsSync, statSync } = require('fs');
      const { join } = require('path');
      const root = 'public';
      const files = [
        'pwa-64x64.png','pwa-72x72.png','pwa-96x96.png','pwa-128x128.png',
        'pwa-144x144.png','pwa-152x152.png','pwa-192x192.png','pwa-384x384.png',
        'pwa-512x512.png','maskable-icon-512x512.png','apple-touch-icon-180x180.png','favicon.ico'
      ];
      let ok = true;
      for (const f of files) {
        const p = join(root, f);
        if (!existsSync(p)) { console.error('MISSING: ' + p); ok = false; continue; }
        const sz = statSync(p).size;
        if (sz < 500) { console.error('TOO SMALL (placeholder?): ' + p + ' = ' + sz + ' bytes'); ok = false; }
        else console.log('OK ' + sz + 'B  ' + p);
      }
      process.exit(ok ? 0 : 1);
      "
    </automated>
    <automated>
      node -e "
      const { readFileSync } = require('fs');
      const svg = readFileSync('public/icon-source.svg', 'utf8');
      const hasFilter = /&lt;filter|feGaussianBlur|feMerge|&lt;mask/.test(svg);
      const hasBg = svg.includes('#1F3A4D');
      const hasWhite = svg.includes('#FFFFFF') || svg.includes('#ffffff') || svg.includes('white');
      const noCSS = !svg.includes('var(--');
      console.log('filter-free:', !hasFilter);
      console.log('navy background:', hasBg);
      console.log('white mark:', hasWhite);
      console.log('no CSS vars:', noCSS);
      if (hasFilter || !hasBg || !hasWhite || !noCSS) process.exit(1);
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - public/icon-source.svg exists with viewBox="0 0 512 512", contains `fill="#1F3A4D"` background rect, contains the lightning-bolt path with `fill="#FFFFFF"`, contains NO `<filter>`, `<feGaussianBlur>`, `<feMerge>`, or `<mask>` elements, contains NO `var(--` CSS variable references
    - All 12 PNG/ICO files exist in public/ with file size > 500 bytes (placeholder was 69 bytes)
    - pwa-192x192.png at public/pwa-192x192.png (root, NOT public/icons/pwa-192x192.png)
    - pwa-512x512.png at public/pwa-512x512.png (root, NOT public/icons/pwa-512x512.png)
    - scripts/generate-icons.mjs exists, is runnable with `node scripts/generate-icons.mjs` without errors
  </acceptance_criteria>
  <done>
    All 12 brand-mark PNG/ICO files exist in public/ at > 500 bytes each; icon-source.svg is filter-free with #1F3A4D background and #FFFFFF mark; generate-icons.mjs runs cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update vite.config.ts manifest, fix index.html, remove old icon placeholders</name>
  <files>
    vite.config.ts
    index.html
  </files>
  <read_first>
    - vite.config.ts — read the full VitePWA() call; note the existing `icons` array (2 entries with wrong paths and anti-pattern purpose) and the existing `workbox.globPatterns` and any `includeAssets` field
    - index.html — read all current `<head>` contents; the current title is "app-tmp" and apple-touch-icon link is missing
    - .planning/phases/07-pwa-brand-icons/07-RESEARCH.md — Pattern 4 (vite.config.ts icons array) and Pattern 5 (index.html head)
  </read_first>
  <action>
    Step 1 — Update vite.config.ts VitePWA() call:

    Add `includeAssets` array immediately after `strategies: 'generateSW'`:
    ```
    includeAssets: [
      'favicon.svg', 'favicon.ico',
      'apple-touch-icon-180x180.png',
      'pwa-64x64.png', 'pwa-72x72.png', 'pwa-96x96.png',
      'pwa-128x128.png', 'pwa-144x144.png', 'pwa-152x152.png',
      'pwa-192x192.png', 'pwa-384x384.png', 'pwa-512x512.png',
      'maskable-icon-512x512.png',
    ],
    ```

    Replace the existing `icons` array inside `manifest:` with EXACTLY these 10 entries (order matters for browser resolution):
    ```
    icons: [
      { src: '/pwa-64x64.png',             sizes: '64x64',   type: 'image/png' },
      { src: '/pwa-72x72.png',             sizes: '72x72',   type: 'image/png' },
      { src: '/pwa-96x96.png',             sizes: '96x96',   type: 'image/png' },
      { src: '/pwa-128x128.png',           sizes: '128x128', type: 'image/png' },
      { src: '/pwa-144x144.png',           sizes: '144x144', type: 'image/png' },
      { src: '/pwa-152x152.png',           sizes: '152x152', type: 'image/png' },
      { src: '/pwa-192x192.png',           sizes: '192x192', type: 'image/png' },
      { src: '/pwa-384x384.png',           sizes: '384x384', type: 'image/png' },
      { src: '/pwa-512x512.png',           sizes: '512x512', type: 'image/png', purpose: 'any' },
      { src: '/maskable-icon-512x512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
    ],
    ```
    Critical: NO entry may have `purpose: 'any maskable'` — that is the anti-pattern being fixed. The 512px `purpose: 'any'` and `maskable-icon-512x512.png` `purpose: 'maskable'` are SEPARATE entries.
    Critical: All src paths start with `/pwa-` at root — NOT `/icons/pwa-`. The old `/icons/` directory is being retired.
    Leave all other VitePWA config unchanged (registerType, strategies, workbox, manifest name/short_name/theme_color/background_color/display/orientation).

    Step 2 — Update index.html `<head>`:

    Keep the existing `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />` line unchanged.

    Add these two new lines directly after the existing favicon link:
    `<link rel="icon" type="image/png" sizes="64x64" href="/pwa-64x64.png" />`
    `<link rel="apple-touch-icon" href="/apple-touch-icon-180x180.png" sizes="180x180" />`
    `<meta name="theme-color" content="#1F3A4D" />`

    Change `<title>app-tmp</title>` to `<title>Bloom — PCOS Nutrition</title>`

    Step 3 — Remove the old icons/ directory and its placeholder files:
    Delete public/icons/pwa-192x192.png (69-byte placeholder)
    Delete public/icons/pwa-512x512.png (69-byte placeholder)
    Delete the public/icons/ directory if now empty

    Use Node.js or PowerShell to remove — do not leave the old placeholder files in place.
    Command: `node -e "const {rmSync}=require('fs'); rmSync('public/icons', {recursive:true, force:true}); console.log('Removed public/icons/')"`

    Step 4 — Run the build to confirm no broken manifest references:
    `npm run build`
    Verify dist/manifest.webmanifest is generated and contains all 10 icon entries.
  </action>
  <verify>
    <automated>
      node -e "
      const { readFileSync, existsSync } = require('fs');
      const cfg = readFileSync('vite.config.ts', 'utf8');
      // Must NOT have old anti-pattern
      if (cfg.includes('any maskable')) { console.error('FAIL: anti-pattern purpose: any maskable still present'); process.exit(1); }
      // Must NOT have old /icons/ paths
      if (cfg.includes('/icons/pwa-')) { console.error('FAIL: old /icons/pwa- path still present'); process.exit(1); }
      // Must have maskable entry
      if (!cfg.includes('maskable-icon-512x512.png')) { console.error('FAIL: maskable-icon entry missing'); process.exit(1); }
      // Must have purpose: 'any' and purpose: 'maskable' separately
      if (!cfg.includes(\"purpose: 'any'\") && !cfg.includes('purpose: \"any\"')) { console.error('FAIL: purpose any entry missing'); process.exit(1); }
      if (!cfg.includes(\"purpose: 'maskable'\") && !cfg.includes('purpose: \"maskable\"')) { console.error('FAIL: purpose maskable entry missing'); process.exit(1); }
      // Must have pwa-72x72 (one of the new sizes)
      if (!cfg.includes('pwa-72x72.png')) { console.error('FAIL: pwa-72x72 entry missing'); process.exit(1); }
      console.log('vite.config.ts manifest icons: OK');
      // Check index.html
      const html = readFileSync('index.html', 'utf8');
      if (!html.includes('apple-touch-icon')) { console.error('FAIL: apple-touch-icon missing from index.html'); process.exit(1); }
      if (!html.includes('Bloom')) { console.error('FAIL: title still app-tmp or missing Bloom'); process.exit(1); }
      if (html.includes('app-tmp')) { console.error('FAIL: title still contains app-tmp'); process.exit(1); }
      if (!html.includes('theme-color')) { console.error('FAIL: theme-color meta missing'); process.exit(1); }
      console.log('index.html: OK');
      // Old placeholder dir must be gone
      if (existsSync('public/icons/pwa-192x192.png')) { console.error('FAIL: old placeholder still at public/icons/pwa-192x192.png'); process.exit(1); }
      console.log('Old placeholder cleanup: OK');
      "
    </automated>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts contains NO instance of `'any maskable'` or `"any maskable"` (the anti-pattern)
    - vite.config.ts contains NO `/icons/pwa-` paths (old path convention)
    - vite.config.ts manifest.icons has exactly 10 entries: 8 without purpose + 1 with purpose 'any' + 1 with purpose 'maskable'
    - vite.config.ts includes `includeAssets` array listing all 13 icon files
    - index.html `<title>` is `Bloom — PCOS Nutrition` (not `app-tmp`)
    - index.html `<head>` contains `<link rel="apple-touch-icon" href="/apple-touch-icon-180x180.png"`
    - index.html `<head>` contains `<meta name="theme-color" content="#1F3A4D"`
    - public/icons/ directory no longer exists (old placeholder files deleted)
    - `npm run build` completes without errors
    - dist/manifest.webmanifest (after build) contains entries for `/pwa-192x192.png` and `/maskable-icon-512x512.png`
  </acceptance_criteria>
  <done>
    vite.config.ts and index.html updated; old placeholder icons/ directory deleted; npm run build succeeds; dist/manifest.webmanifest references real brand icon files at /pwa-*.png root paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDependency | @vite-pwa/assets-generator installed as a devDependency; runs at build time only, not in production bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | npm install @vite-pwa/assets-generator | mitigate | Package verified [OK] in RESEARCH.md Package Legitimacy Audit (npm 1.0.2, vite-pwa org, created 2023-06-05); sharp [OK] (13yr-old package, 50M+/wk downloads) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Both packages rated [OK] in RESEARCH.md audit — no [ASSUMED] or [SUS] packages in this phase |

Note: This phase makes no changes to authentication, data handling, API routes, or network requests. Icons are static public files. No further STRIDE categories apply per RESEARCH.md security domain analysis.
</threat_model>

<verification>
After both tasks complete:

1. File existence check — all brand mark PNGs in public/:
   - Run: `node -e "['pwa-64x64','pwa-72x72','pwa-96x96','pwa-128x128','pwa-144x144','pwa-152x152','pwa-192x192','pwa-384x384','pwa-512x512','maskable-icon-512x512','apple-touch-icon-180x180'].forEach(f => { const s=require('fs').statSync('public/'+f+'.png').size; console.log(s>500?'OK':'FAIL', s+'B', f) })"`
   - Expected: all files > 500 bytes (placeholder was 69 bytes)

2. SVG source integrity — no filters in icon-source.svg:
   - Run: `node -e "const s=require('fs').readFileSync('public/icon-source.svg','utf8'); console.log('filter-free:', !/feGaussianBlur/.test(s))"`
   - Expected: `filter-free: true`

3. Manifest correctness — no anti-pattern, correct paths:
   - Run: `node -e "const c=require('fs').readFileSync('vite.config.ts','utf8'); console.log('no anti-pattern:', !c.includes('any maskable')); console.log('no old path:', !c.includes('/icons/pwa-'))"`
   - Expected: both `true`

4. Build succeeds:
   - Run: `npm run build`
   - Expected: exits 0, dist/ created

5. Manifest in dist:
   - After build: check dist/manifest.webmanifest contains `/pwa-192x192.png` and `/maskable-icon-512x512.png`

6. index.html correctness:
   - Check: title is "Bloom — PCOS Nutrition", apple-touch-icon link present, theme-color meta present
</verification>

<success_criteria>
- All 12 brand-mark PNG/ICO files exist in public/ with size > 500 bytes (replacing 69-byte placeholders)
- public/icon-source.svg is filter-free: no `feGaussianBlur`, no CSS variables, solid #1F3A4D background, white lightning-bolt mark
- vite.config.ts manifest.icons has 10 entries with /pwa-*.png root paths; zero instances of `purpose: 'any maskable'`; includes `includeAssets` listing all icon files
- index.html title is "Bloom — PCOS Nutrition"; apple-touch-icon link and theme-color meta are present
- public/icons/ directory is deleted (old placeholders gone)
- `npm run build` exits 0 — the built app references real icons
</success_criteria>

<output>
Create `.planning/phases/07-pwa-brand-icons/07-01-SUMMARY.md` when done.

The summary must record:
- The exact lightning-bolt path transform used in icon-source.svg
- The @vite-pwa/assets-generator version installed
- Confirmation that the minimal-2023 preset output files were generated to public/ root
- Final manifest.icons entry count (10)
- Any visual verification notes from inspecting the generated PNGs
</output>
