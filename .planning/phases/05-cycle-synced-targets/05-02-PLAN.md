---
phase: 05-cycle-synced-targets
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/hooks/useProfile.ts
  - src/hooks/useAITargets.ts
  - src/components/home/CyclePhaseChip.tsx
  - src/components/home/LutealTipCard.tsx
  - src/screens/home/HomeScreen.tsx
autonomous: true
requirements:
  - CYCL-02
  - CYCL-03
  - CYCL-04
  - CYCL-05

must_haves:
  truths:
    - "useProfile hook returns last_period_date, cycle_length_days, period_length_days in the profile data"
    - "useAITargets regenerates targets when the computed cycle phase differs from ai_daily_targets.cycle_phase (phase-drift check)"
    - "useAITargets also regenerates when PROMPT_VERSION of stored row is 1 (stale from pre-Phase-5)"
    - "CyclePhaseChip renders a pill showing Phase Name · Day N below the date header"
    - "Tapping the CyclePhaseChip opens a bottom sheet with a 3–4 sentence per-phase explanation"
    - "CyclePhaseChip is hidden when profile.last_period_date is null"
    - "LutealTipCard renders only during Luteal phase, below macro rings, above meal log"
    - "LutealTipCard shows heading 'Luteal phase cravings incoming', 3 comfort food swaps, and a CTA navigating to /recipes?phase=luteal"
    - "Tapping 'Explore PCOS-friendly recipes' from LutealTipCard navigates to /recipes?phase=luteal"
  artifacts:
    - path: "src/hooks/useProfile.ts"
      provides: "Profile query including cycle columns"
      contains: "last_period_date"
    - path: "src/hooks/useAITargets.ts"
      provides: "Phase-drift check + PROMPT_VERSION staleness check"
      contains: "cycle_phase"
    - path: "src/components/home/CyclePhaseChip.tsx"
      provides: "Phase pill + bottom sheet"
      exports: ["CyclePhaseChip"]
    - path: "src/components/home/LutealTipCard.tsx"
      provides: "Luteal craving interception card"
      exports: ["LutealTipCard"]
    - path: "src/screens/home/HomeScreen.tsx"
      provides: "Dashboard with CyclePhaseChip and conditional LutealTipCard"
      contains: "CyclePhaseChip"
  key_links:
    - from: "src/hooks/useAITargets.ts"
      to: "getCyclePhase from src/lib/cycle.ts"
      via: "import getCyclePhase and call with profile.last_period_date, profile.cycle_length_days"
    - from: "src/hooks/useAITargets.ts"
      to: "ai_daily_targets.cycle_phase column"
      via: "select cycle_phase from the stored row and compare to computed phase"
    - from: "src/components/home/LutealTipCard.tsx"
      to: "/recipes?phase=luteal"
      via: "useNavigate() navigate('/recipes?phase=luteal')"
    - from: "src/screens/home/HomeScreen.tsx"
      to: "CyclePhaseChip"
      via: "rendered below date header, hidden when profile.last_period_date is null"
---

## Phase Goal

**As a** woman with PCOS, **I want to** see my current cycle phase on my dashboard and have my nutrition targets automatically refresh when my phase changes, **so that** I always act on targets calibrated to where I am in my cycle.

<objective>
Wire cycle-awareness into the dashboard: updated hooks (useProfile returns cycle fields, useAITargets gains phase-drift check and PROMPT_VERSION=1 staleness), two new home components (CyclePhaseChip, LutealTipCard), and HomeScreen integration.

Purpose: After this plan the Today dashboard shows the user's cycle phase pill, tapping it opens a brief explanation, Luteal phase surfaces a craving-interception card, and stale (v1 or wrong-phase) targets automatically trigger regeneration.

Output: Updated hooks, two new components in src/components/home/, updated HomeScreen.
</objective>

<execution_context>
@C:\Users\sunar\Desktop\Bloom_APP\app\.planning\phases\05-cycle-synced-targets\05-CONTEXT.md
</execution_context>

<context>
@C:\Users\sunar\Desktop\Bloom_APP\app\.planning\ROADMAP.md
@C:\Users\sunar\Desktop\Bloom_APP\app\.planning\phases\05-cycle-synced-targets\05-CONTEXT.md
@C:\Users\sunar\Desktop\Bloom_APP\app\CLAUDE.md

<interfaces>
<!-- From src/hooks/useAITargets.ts (read before modifying) -->
export type AITargets = {
  protein_g: number; fiber_g: number; gl_target: number;
  added_sugar_g: number; calorie_min: number; calorie_max: number;
  pc_ratio_target: number; insulin_score: number | null;
  narrative: string; generated_at: string; source: 'ai' | 'static'
}

// Stale check (existing pattern to extend — do NOT remove):
const needsRegen =
  !existing ||
  (profileUpdatedAt != null && existing.generated_at < profileUpdatedAt)

// New phase-drift check to add (runs before existing check):
// if existing.cycle_phase !== computedPhase AND profile.last_period_date is set → needsRegen
// if existing.prompt_version < 2 → needsRegen

<!-- From src/hooks/useProfile.ts (currently selects '*') -->
// useProfile returns supabase.from('profiles').select('*')
// After Phase 5-01 db push, '*' already returns cycle columns.
// Update the select to be explicit: include last_period_date, cycle_length_days, period_length_days.

<!-- From src/lib/cycle.ts (created in Plan 01) -->
export type CyclePhase = 'menstrual' | 'follicular' | 'ovulation' | 'luteal'
export type CyclePhaseResult = { phase: CyclePhase; cycleDay: number; cycleLength: number }
export function getCyclePhase(lastPeriodDate: Date | null, cycleLengthDays: number): CyclePhaseResult | null

<!-- From src/lib/cycleContent.ts (created in Plan 01) -->
export const PHASE_CONTENT: Record<CyclePhase, { title: string; chipLabel: string; explanation: string }>
export const LUTEAL_SWAPS: ReadonlyArray<{ craving: string; swap: string; glNote: string }>

<!-- From src/components/ui/ — available primitives -->
Card({ children, pad?, className })     // bg-b-surface, rounded-b-md, shadow-b-card, border-b-hairline
Chip({ children, tone, size?, icon?, onClick?, className? })
// tone options: 'primary' | 'mint' | 'amber' | 'neutral' | 'ghost' | 'accent'

<!-- From src/screens/home/HomeScreen.tsx — current render order -->
// Header (date label + greeting + Log food button)
// Scrollable body:
//   DateStrip
//   MacroProgressCard
//   MealSlotsSection
//   InsulBalanceCard
//   SymptomSummaryCard
//   WeightCard
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update useProfile and useAITargets hooks with cycle awareness</name>
  <files>src/hooks/useProfile.ts, src/hooks/useAITargets.ts</files>
  <read_first>
    - src/hooks/useProfile.ts (full current file)
    - src/hooks/useAITargets.ts (full current file — understand existing stale check before extending)
    - src/lib/cycle.ts (getCyclePhase signature and return type — created in Plan 01)
    - src/lib/database.types.ts (verify Tables<'profiles'> and Tables<'ai_daily_targets'> now include new columns)
  </read_first>
  <action>
    CHANGE 1 — useProfile.ts:
    Keep the existing useQuery structure. Change the select clause from '*' to an explicit list that includes the cycle fields:
      supabase.from('profiles').select('id, display_name, age, height_cm, current_weight_kg, goal_weight_kg, pcos_type, goals, palette, dark_mode, created_at, updated_at, last_period_date, cycle_length_days, period_length_days')
    This makes the return type precise for consumers of useProfile. No other changes.

    CHANGE 2 — useAITargets.ts:
    Import getCyclePhase from '@/lib/cycle' at the top of the file.

    In the queryFn, after the parallel fetch (targetsRes, profileRes):

    a. Extend the profile select to include cycle fields. Change:
         supabase.from('profiles').select('updated_at')
       to:
         supabase.from('profiles').select('updated_at, last_period_date, cycle_length_days')

    b. Extract cycle data from profileRes:
         const lastPeriodDateStr = profileRes.data?.last_period_date ?? null
         const cycleLengthDays = profileRes.data?.cycle_length_days ?? 28
         const lastPeriodDate = lastPeriodDateStr ? new Date(lastPeriodDateStr) : null
         const cycleResult = getCyclePhase(lastPeriodDate, cycleLengthDays)
         const computedPhase = cycleResult?.phase ?? null

    c. Extend the needsRegen check. Add two new conditions BEFORE the existing profile-updated-at check:
         // Phase drift: computed phase differs from stored phase
         const phaseDrifted =
           computedPhase !== null &&
           existing != null &&
           (existing as Record<string, unknown>).cycle_phase !== computedPhase
         // Stale prompt version: row was generated before Phase 5 (prompt_version < 2)
         const promptVersionStale =
           existing != null &&
           ((existing as Record<string, unknown>).prompt_version as number ?? 0) < 2

       Combine with existing check:
         const needsRegen =
           !existing ||
           phaseDrifted ||
           promptVersionStale ||
           (profileUpdatedAt != null && existing.generated_at < profileUpdatedAt)

    d. Add cycle_phase to the AITargets type and rowToTargets mapper:
       - In AITargets type: add cycle_phase?: string | null
       - In rowToTargets: add cycle_phase: typeof row.cycle_phase === 'string' ? row.cycle_phase : null

    Keep all existing fallback and retry logic unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - useProfile.ts select clause explicitly includes 'last_period_date', 'cycle_length_days', 'period_length_days'
    - useAITargets.ts imports getCyclePhase from '@/lib/cycle'
    - useAITargets.ts queryFn fetches 'updated_at, last_period_date, cycle_length_days' from profiles
    - phaseDrifted: true when computedPhase !== null AND existing.cycle_phase !== computedPhase
    - promptVersionStale: true when existing.prompt_version < 2
    - needsRegen includes phaseDrifted OR promptVersionStale conditions
    - AITargets type includes cycle_phase?: string | null field
    - npx tsc --noEmit exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>useProfile returns cycle fields. useAITargets regenerates when phase drifts or prompt_version is stale (< 2). TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create CyclePhaseChip and LutealTipCard components</name>
  <files>src/components/home/CyclePhaseChip.tsx, src/components/home/LutealTipCard.tsx</files>
  <read_first>
    - src/lib/cycleContent.ts (PHASE_CONTENT and LUTEAL_SWAPS — exact copy to render)
    - src/lib/cycle.ts (CyclePhase, CyclePhaseResult types)
    - src/components/ui/Card.tsx (Card component signature)
    - src/components/ui/Chip.tsx (Chip component signature and tone options)
    - bloom-app-design/project/tokens.css (design token names for colors and spacing)
  </read_first>
  <action>
    Create src/components/home/CyclePhaseChip.tsx:

    Props: { result: CyclePhaseResult }
    Import CyclePhaseResult from '@/lib/cycle' and PHASE_CONTENT from '@/lib/cycleContent'.
    Import useState from 'react'.

    Render a pill button showing `{phase title} · Day {cycleDay}` inline.
    Styling: small pill (rounded-full), border border-b-hairline, bg-b-surface, px-3 py-1, text-[11px] font-semibold text-b-ink-2.
    On tap: set isOpen=true, show the bottom sheet.

    Bottom sheet implementation (inline, no external library needed — check src/components/ui/ for Sheet first, use it if available, otherwise build inline):
    - Fixed overlay: fixed inset-0 z-50 flex items-end, background rgba(0,0,0,0.4), onClick closes sheet
    - Sheet panel: w-full max-w-screen-sm mx-auto bg-b-surface rounded-t-2xl px-5 pt-4 pb-8
    - Drag indicator: mx-auto mb-4 h-1 w-10 rounded-full bg-b-surface-sunken
    - Title: text-[17px] font-bold text-b-ink (PHASE_CONTENT[result.phase].title)
    - Explanation: text-[13px] leading-relaxed text-b-ink-2 mt-3 (PHASE_CONTENT[result.phase].explanation)
    - Close button: mt-5 w-full py-3 rounded-b-pill bg-b-primary text-b-primary-ink text-[14px] font-bold

    Export as named export: export function CyclePhaseChip({ result }: { result: CyclePhaseResult })

    ---

    Create src/components/home/LutealTipCard.tsx:

    Props: none (reads LUTEAL_SWAPS from cycleContent)
    Import Card from '@/components/ui'.
    Import LUTEAL_SWAPS from '@/lib/cycleContent'.
    Import useNavigate from 'react-router-dom'.

    Render structure inside a Card (className="p-4"):
    - Heading row: small label "LUTEAL PHASE" (text-[10px] font-bold uppercase tracking-[0.4px] text-b-ink-3), then heading "Luteal phase cravings incoming" (text-[15px] font-bold text-b-ink mt-0.5)
    - Subtitle: "Progesterone-driven cravings? Try these low-GL swaps:" (text-[12px] text-b-ink-2 mt-2)
    - Swap rows: for each LUTEAL_SWAPS entry render a row (flex items-center gap-2 mt-2.5):
        - Craving label: text-[12px] font-semibold text-b-ink-2, content "Craving {swap.craving}?"
        - Arrow: text-b-ink-3 "→"
        - Swap text: text-[12px] font-bold text-b-ink, content "{swap.swap}"
        - GL badge: inline span, text-[10px] font-semibold text-b-mint bg-b-mint-soft rounded-full px-1.5 py-0.5, content "{swap.glNote}"
    - CTA button (mt-4, full width):
        Button (not Btn component — use a plain button element to keep it lightweight):
        className: "w-full rounded-b-pill border border-b-primary py-2.5 text-[13px] font-bold text-b-primary active:opacity-70"
        text: "Explore PCOS-friendly recipes →"
        onClick: navigate('/recipes?phase=luteal')

    Export as named export: export function LutealTipCard()

    CRITICAL: Use className prop only. No style prop on any element (CLAUDE.md rule 8).
    Use 'var(--b-mint)' via Tailwind class equivalents (text-b-mint, bg-b-mint-soft etc.) — never hardcode hex.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - CyclePhaseChip.tsx exports CyclePhaseChip function component accepting { result: CyclePhaseResult } prop
    - CyclePhaseChip renders a pill with text matching "{PHASE_CONTENT[result.phase].title} · Day {result.cycleDay}"
    - Tapping the pill sets isOpen=true and renders the bottom sheet
    - Bottom sheet displays PHASE_CONTENT[result.phase].explanation text
    - LutealTipCard.tsx exports LutealTipCard function component
    - LutealTipCard renders heading "Luteal phase cravings incoming"
    - LutealTipCard renders exactly 3 swap rows (one per LUTEAL_SWAPS entry)
    - LutealTipCard CTA button onClick calls navigate('/recipes?phase=luteal')
    - No style prop used on any element in either file (className only)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>CyclePhaseChip renders phase pill and bottom sheet with per-phase explanation. LutealTipCard renders 3 food swaps and CTA routing to /recipes?phase=luteal. Both components type-check cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CyclePhaseChip and LutealTipCard into HomeScreen</name>
  <files>src/screens/home/HomeScreen.tsx</files>
  <read_first>
    - src/screens/home/HomeScreen.tsx (full current file — understand current render order before adding)
    - src/components/home/CyclePhaseChip.tsx (prop shape: result: CyclePhaseResult)
    - src/components/home/LutealTipCard.tsx (no props)
    - src/hooks/useProfile.ts (confirm it now returns last_period_date, cycle_length_days)
    - src/lib/cycle.ts (getCyclePhase signature)
  </read_first>
  <action>
    Add imports at the top of HomeScreen.tsx:
      import { getCyclePhase } from '@/lib/cycle'
      import { CyclePhaseChip } from '@/components/home/CyclePhaseChip'
      import { LutealTipCard } from '@/components/home/LutealTipCard'

    In the HomeScreen function body, after destructuring profile data, compute the cycle phase:
      const cycleResult = getCyclePhase(
        profile?.last_period_date ? new Date(profile.last_period_date) : null,
        profile?.cycle_length_days ?? 28,
      )

    In the JSX, make two insertions:

    INSERTION 1 — CyclePhaseChip below the date label, inside the header div (below the existing `<p className="text-[11px]...">` date label line, before the greeting h1):
      {cycleResult && <CyclePhaseChip result={cycleResult} />}

    INSERTION 2 — LutealTipCard in the scrollable body, AFTER MacroProgressCard and BEFORE MealSlotsSection:
      {cycleResult?.phase === 'luteal' && <LutealTipCard />}

    The current render order in the scrollable body is:
      DateStrip → MacroProgressCard → MealSlotsSection → InsulBalanceCard → SymptomSummaryCard → WeightCard

    After change, the order becomes:
      DateStrip → MacroProgressCard → LutealTipCard (conditional) → MealSlotsSection → InsulBalanceCard → SymptomSummaryCard → WeightCard

    Do not modify any existing components or their props. Only add the two new insertions and the cycleResult computation.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - HomeScreen.tsx imports getCyclePhase, CyclePhaseChip, LutealTipCard
    - cycleResult = getCyclePhase(profile?.last_period_date ? new Date(profile.last_period_date) : null, profile?.cycle_length_days ?? 28)
    - {cycleResult && <CyclePhaseChip result={cycleResult} />} rendered in the header section
    - {cycleResult?.phase === 'luteal' && <LutealTipCard />} rendered after MacroProgressCard, before MealSlotsSection
    - When profile.last_period_date is null, cycleResult is null and neither component renders
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>HomeScreen renders CyclePhaseChip in header (hidden when no period date) and conditionally renders LutealTipCard between macro rings and meal log during Luteal phase. TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile data → getCyclePhase | last_period_date from DB, parsed as Date client-side |
| ai_daily_targets.cycle_phase → phase-drift check | Stored text compared to locally computed phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | getCyclePhase with malformed last_period_date | mitigate | new Date(lastPeriodDateStr) returns Invalid Date if malformed; Math.floor of NaN is NaN; cycleDay = (NaN % 28) + 1 = NaN; phase conditions all fail; function should guard: if isNaN(lastPeriodDate.getTime()) return null |
| T-05-06 | Information Disclosure | CyclePhaseChip bottom sheet | accept | Displays only static educational copy, no personal data in the UI text |
| T-05-07 | Elevation of Privilege | promptVersionStale triggers regen for all users on first load | accept | Regeneration is one-time per user, guarded by staleTime:5min; cost is bounded |
| T-05-SC | Tampering | No new npm/pip installs in this plan | accept | No new package manager installs |
</threat_model>

<verification>
After completing all tasks:
1. Run: npx tsc --noEmit (must pass with 0 errors)
2. Run: grep -n "cycle_phase" src/hooks/useAITargets.ts | head -5 (expect phaseDrifted, promptVersionStale, cycle_phase in select)
3. Run: grep -n "getCyclePhase" src/screens/home/HomeScreen.tsx (expect import and call)
4. Run: grep -n "LutealTipCard" src/screens/home/HomeScreen.tsx (expect import and conditional render)
5. Manual verify: Open dev server, profile with null last_period_date → no chip visible. Set a date 21 days ago with 28-day cycle → chip shows "Luteal · Day 21" and LutealTipCard appears.
</verification>

<success_criteria>
- useProfile returns last_period_date, cycle_length_days, period_length_days
- useAITargets regenerates when phase drifts or prompt_version < 2 (in addition to existing profile-change trigger)
- CyclePhaseChip renders pill and bottom sheet with correct per-phase copy
- LutealTipCard renders 3 swaps and routes to /recipes?phase=luteal
- HomeScreen shows chip in header, Luteal card between macro rings and meal log
- npx tsc --noEmit exits 0
</success_criteria>

<output>
Create .planning/phases/05-cycle-synced-targets/05-02-SUMMARY.md when done.
</output>
