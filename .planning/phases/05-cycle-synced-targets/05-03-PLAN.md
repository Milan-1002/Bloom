---
phase: 05-cycle-synced-targets
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/hooks/useUpsertSymptomLog.ts
  - src/screens/symptoms/SymptomsScreen.tsx
  - src/screens/profile/ProfileScreen.tsx
  - src/screens/recipes/RecipesScreen.tsx
autonomous: true
requirements:
  - CYCL-01
  - CYCL-04
  - CYCL-05

must_haves:
  truths:
    - "SymptomsScreen renders a Cravings row (1–5 dot bar) in the 'Other symptoms' card alongside mood/sleep/bloating/skin"
    - "Cravings value is persisted to symptom_logs.cravings via useUpsertSymptomLog"
    - "ProfileScreen renders a 'My Cycle' card with a date input for last_period_date and steppers for cycle_length_days and period_length_days"
    - "Saving My Cycle data updates profiles via supabase.from('profiles').update() with the new cycle fields and touches updated_at"
    - "Updating last_period_date or cycle_length_days triggers updated_at change which propagates to useAITargets regeneration (per D-02)"
    - "RecipesScreen reads the ?phase=luteal query param on mount and pre-selects the 'Low GL' filter when phase=luteal"
    - "My Cycle section is hidden or gracefully disabled when the user has not set a period date (null shows a prompt to add date)"
  artifacts:
    - path: "src/hooks/useUpsertSymptomLog.ts"
      provides: "Mutation with cravings field included in upsert payload"
      contains: "cravings"
    - path: "src/screens/symptoms/SymptomsScreen.tsx"
      provides: "Cravings SymDef entry in SYMPTOMS array, cravings in SymValues type"
      contains: "cravings"
    - path: "src/screens/profile/ProfileScreen.tsx"
      provides: "My Cycle card with date picker and cycle length steppers"
      contains: "last_period_date"
    - path: "src/screens/recipes/RecipesScreen.tsx"
      provides: "?phase=luteal query param pre-applies Low GL filter"
      contains: "useSearchParams"
  key_links:
    - from: "ProfileScreen My Cycle save"
      to: "profiles.updated_at"
      via: "supabase.from('profiles').update({ last_period_date, cycle_length_days, period_length_days, updated_at: new Date().toISOString() })"
    - from: "profiles.updated_at change"
      to: "useAITargets regeneration"
      via: "existing stale check: existing.generated_at < profileUpdatedAt (Plan 02 preserved this)"
    - from: "RecipesScreen useSearchParams"
      to: "activeFilter state"
      via: "phase=luteal → setActiveFilter('Low GL') on mount via useEffect"
---

## Phase Goal

**As a** woman with PCOS, **I want to** log my period start date in Profile settings, track my cravings in the Symptoms screen, and have the Recipes screen pre-filter to low-GL options when I tap the Luteal tip card, **so that** the app collects my cycle data and helps me make better food choices during high-craving phases.

<objective>
Complete the user-facing cycle data entry and data capture layer: My Cycle section in Profile, Cravings symptom tracking, and Recipes screen URL-driven filter. These three features are independent of each other (no shared files) and can run in parallel with Plan 02 (wave 2).

Purpose: After this plan a user can enter their period start date (which triggers target regeneration via the existing profile-change path), log cravings alongside other symptoms, and land on low-GL recipes from the Luteal tip card CTA.

Output: Updated useUpsertSymptomLog, updated SymptomsScreen and ProfileScreen, updated RecipesScreen.
</objective>

<execution_context>
@C:\Users\sunar\Desktop\Bloom_APP\app\.planning\phases\05-cycle-synced-targets\05-CONTEXT.md
</execution_context>

<context>
@C:\Users\sunar\Desktop\Bloom_APP\app\.planning\ROADMAP.md
@C:\Users\sunar\Desktop\Bloom_APP\app\.planning\phases\05-cycle-synced-targets\05-CONTEXT.md
@C:\Users\sunar\Desktop\Bloom_APP\app\CLAUDE.md

<interfaces>
<!-- From src/hooks/useUpsertSymptomLog.ts (existing — extend) -->
export interface SymptomPayload {
  log_date: string
  energy: number; mood: number; sleep: number; bloating: number; skin: number
  // Add: cravings: number
}

// In mutationFn, the n() helper coerces 0 to null for DB check constraint.
// Apply same pattern to cravings: cravings: n(payload.cravings)

<!-- From src/screens/symptoms/SymptomsScreen.tsx (existing patterns) -->
type SymValues = { energy: number; mood: number; sleep: number; bloating: number; skin: number }
// Extend to: cravings: number

const EMPTY: SymValues = { energy: 0, mood: 0, sleep: 0, bloating: 0, skin: 0 }
// Extend to include: cravings: 0

// SYMPTOMS array pattern (SymDef):
interface SymDef {
  key: 'mood' | 'sleep' | 'bloating' | 'skin'  // union — add 'cravings'
  label: string
  color: string
  Icon: () => JSX.Element
}

// VALUE_LABELS pattern for dot-bar caption:
const VALUE_LABELS: Record<string, Record<number, string>> = {
  mood: { 0: '—', 1: 'Down', ... },
  // Add cravings: { 0: '—', 1: 'None', 2: 'Mild', 3: 'Moderate', 4: 'Strong', 5: 'Intense' }
}

// SymptomDotBar component reads VALUE_LABELS[sym.key] — adding 'cravings' to SYMPTOMS automatically wires it.

// useEffect sync pattern:
useEffect(() => {
  if (!isFetching) {
    setValues({
      energy: savedLog?.energy ?? 0,
      mood: savedLog?.mood ?? 0,
      // ... add cravings: savedLog?.cravings ?? 0
    })
  }
}, [savedLog, isFetching])

<!-- From src/screens/profile/ProfileScreen.tsx (existing patterns) -->
// Section structure: label div + Card div
// Existing sections: "Appearance" (theme) and "Account" (delete)
// Add "My Cycle" section between Appearance and Account.
// Profile updates use supabase.from('profiles').update({...}).eq('id', user.id)
// CRITICAL: update must include updated_at: new Date().toISOString() to trigger useAITargets invalidation (D-02)

<!-- From src/screens/recipes/RecipesScreen.tsx (existing) -->
type Filter = 'All' | 'Low GL' | 'High protein' | 'High fiber' | 'Anti-inflam' | 'Under 30 min'
const [activeFilter, setActiveFilter] = useState<Filter>('All')

// Needs: import { useSearchParams } from 'react-router-dom'
// On mount: if searchParam phase === 'luteal', setActiveFilter('Low GL')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Cravings to symptom tracking (hook + screen)</name>
  <files>src/hooks/useUpsertSymptomLog.ts, src/screens/symptoms/SymptomsScreen.tsx</files>
  <read_first>
    - src/hooks/useUpsertSymptomLog.ts (full current file — SymptomPayload interface and mutation pattern)
    - src/screens/symptoms/SymptomsScreen.tsx (full current file — SymDef, SYMPTOMS array, SymValues type, VALUE_LABELS, useEffect sync, handleSave)
    - src/lib/database.types.ts (confirm Tables<'symptom_logs'> now has cravings field after Plan 01 migration)
  </read_first>
  <action>
    CHANGE 1 — useUpsertSymptomLog.ts:
    Add cravings: number to the SymptomPayload interface.
    In the mutationFn upsert object, add: cravings: n(payload.cravings)
    No other changes.

    CHANGE 2 — SymptomsScreen.tsx:
    a. Extend the SymDef key union: key: 'mood' | 'sleep' | 'bloating' | 'skin' | 'cravings'

    b. Add to VALUE_LABELS:
         cravings: { 0: '—', 1: 'None', 2: 'Mild', 3: 'Moderate', 4: 'Strong', 5: 'Intense' }

    c. Add a new SymDef entry to the SYMPTOMS array (append after skin, before the closing bracket):
         {
           key: 'cravings', label: 'Cravings', color: 'var(--b-berry)',
           Icon: () => (
             <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
               <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2z" />
               <path d="M8 14s1.5 2 4 2 4-2 4-2" />
               <line x1="9" y1="9" x2="9.01" y2="9" />
               <line x1="15" y1="9" x2="15.01" y2="9" />
             </svg>
           ),
         }
       Note: --b-berry is the existing berry token used in CATEGORIES for Anti-inflam. If --b-berry is not defined in tokens.css, use 'var(--b-accent)' as fallback — check bloom-app-design/project/tokens.css before choosing.

    d. Extend SymValues type: add cravings: number

    e. Extend EMPTY: add cravings: 0

    f. Extend the useEffect sync (setValues call): add cravings: savedLog?.cravings ?? 0

    g. Extend handleSave (the upsert.mutateAsync call already spreads ...values so cravings will be included automatically if it's in SymValues). Verify this is true — if mutateAsync receives typed SymValues and SymptomPayload now includes cravings, no extra change needed.

    h. The hasAnyValue check uses Object.values(values).some(v => v > 0) — cravings is included automatically.
    i. The hasChanges check uses Object.keys(values) — cravings is included automatically.

    IMPORTANT: The SymptomDotBar component renders VALUE_LABELS[sym.key] for the caption. Since cravings is added to VALUE_LABELS, the existing SymptomDotBar will display "None/Mild/Moderate/Strong/Intense" captions correctly.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - SymptomPayload in useUpsertSymptomLog.ts includes cravings: number field
    - Upsert object includes cravings: n(payload.cravings)
    - SymValues type in SymptomsScreen.tsx includes cravings: number
    - SYMPTOMS array has 5 entries (mood, sleep, bloating, skin, cravings)
    - VALUE_LABELS has 'cravings' key with values for 0–5
    - EMPTY object has cravings: 0
    - useEffect sync includes cravings: savedLog?.cravings ?? 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Cravings tracked end-to-end: SymValues type, SYMPTOMS array, VALUE_LABELS, sync useEffect, and upsert payload all include cravings. The Symptoms screen renders a Cravings dot-bar row. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add My Cycle section to ProfileScreen</name>
  <files>src/screens/profile/ProfileScreen.tsx</files>
  <read_first>
    - src/screens/profile/ProfileScreen.tsx (full current file — understand existing state management, supabase update pattern, section layout)
    - src/hooks/useProfile.ts (confirm it returns last_period_date, cycle_length_days, period_length_days after Plan 02)
    - bloom-app-design/project/tokens.css (check available color tokens for the section)
  </read_first>
  <action>
    Add state variables for cycle fields and a saving state inside ProfileScreen (alongside existing palette/dark/deleteStep state):
      const [lastPeriodDate, setLastPeriodDate] = useState<string>('')
      const [cycleLength, setCycleLength] = useState<number>(28)
      const [periodLength, setPeriodLength] = useState<number>(5)
      const [cycleSaving, setCycleSaving] = useState(false)
      const [cycleSaved, setCycleSaved] = useState(false)

    Initialize from profile data using a useEffect that fires when profile changes:
      useEffect(() => {
        if (profile) {
          setLastPeriodDate(profile.last_period_date ?? '')
          setCycleLength(profile.cycle_length_days ?? 28)
          setPeriodLength(profile.period_length_days ?? 5)
        }
      }, [profile])

    Add a save handler:
      const handleSaveCycle = async () => {
        setCycleSaving(true)
        await supabase.from('profiles').update({
          last_period_date: lastPeriodDate || null,
          cycle_length_days: cycleLength,
          period_length_days: periodLength,
          updated_at: new Date().toISOString(),
        }).eq('id', user!.id)
        setCycleSaving(false)
        setCycleSaved(true)
        setTimeout(() => setCycleSaved(false), 2000)
      }

    CRITICAL: updated_at must be set explicitly in the update payload. This is what triggers the useAITargets regeneration (D-02 — the stale check compares existing.generated_at < profileUpdatedAt). Without it, targets will not regenerate on cycle change.

    Add the "My Cycle" section to the JSX, between the Appearance section and the Account section:
      <div>
        <p className="mb-3 text-[10px] font-bold uppercase tracking-widest text-b-ink-3">
          My Cycle
        </p>
        <Card className="p-4">
          {/* Last period date */}
          <div className="mb-4">
            <p className="text-sm font-semibold text-b-ink mb-1">Last period started</p>
            <input
              type="date"
              value={lastPeriodDate}
              max={new Date().toISOString().split('T')[0]}
              onChange={e => setLastPeriodDate(e.target.value)}
              className="w-full rounded-b-md border border-b-hairline bg-b-bg px-3 py-2 text-sm text-b-ink outline-none focus:border-b-primary"
            />
            {!lastPeriodDate && (
              <p className="mt-1 text-[11px] text-b-ink-3">Add your last period date to unlock cycle-synced targets.</p>
            )}
          </div>

          {/* Cycle length stepper */}
          <div className="flex items-center justify-between border-t border-b-hairline pt-4 pb-4">
            <div>
              <p className="text-sm font-semibold text-b-ink">Typical cycle length</p>
              <p className="text-[11px] text-b-ink-3">Days from period start to next period</p>
            </div>
            <div className="flex items-center gap-3">
              <button
                onClick={() => setCycleLength(v => Math.max(21, v - 1))}
                className="flex h-8 w-8 items-center justify-center rounded-full border border-b-hairline bg-b-surface-2 text-b-ink active:opacity-70"
                aria-label="Decrease cycle length"
              >−</button>
              <span className="w-8 text-center text-sm font-bold tabular-nums text-b-ink">{cycleLength}</span>
              <button
                onClick={() => setCycleLength(v => Math.min(45, v + 1))}
                className="flex h-8 w-8 items-center justify-center rounded-full border border-b-hairline bg-b-surface-2 text-b-ink active:opacity-70"
                aria-label="Increase cycle length"
              >+</button>
            </div>
          </div>

          {/* Period length stepper */}
          <div className="flex items-center justify-between border-t border-b-hairline pt-4 pb-2">
            <div>
              <p className="text-sm font-semibold text-b-ink">Period length</p>
              <p className="text-[11px] text-b-ink-3">How many days your period lasts</p>
            </div>
            <div className="flex items-center gap-3">
              <button
                onClick={() => setPeriodLength(v => Math.max(3, v - 1))}
                className="flex h-8 w-8 items-center justify-center rounded-full border border-b-hairline bg-b-surface-2 text-b-ink active:opacity-70"
                aria-label="Decrease period length"
              >−</button>
              <span className="w-8 text-center text-sm font-bold tabular-nums text-b-ink">{periodLength}</span>
              <button
                onClick={() => setPeriodLength(v => Math.min(8, v + 1))}
                className="flex h-8 w-8 items-center justify-center rounded-full border border-b-hairline bg-b-surface-2 text-b-ink active:opacity-70"
                aria-label="Increase period length"
              >+</button>
            </div>
          </div>

          {/* Save button */}
          <button
            onClick={handleSaveCycle}
            disabled={cycleSaving}
            className="mt-4 w-full rounded-b-pill bg-b-primary py-2.5 text-[13px] font-bold text-b-primary-ink active:opacity-70 disabled:opacity-50"
          >
            {cycleSaving ? 'Saving…' : cycleSaved ? 'Saved ✓' : 'Save cycle data'}
          </button>
        </Card>
      </div>

    Use className prop only. No style prop. No hardcoded hex colors.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ProfileScreen.tsx contains state variables: lastPeriodDate, cycleLength, periodLength, cycleSaving, cycleSaved
    - useEffect syncs state from profile (last_period_date, cycle_length_days, period_length_days)
    - handleSaveCycle calls supabase.from('profiles').update({ last_period_date, cycle_length_days, period_length_days, updated_at: new Date().toISOString() })
    - updated_at is explicitly set in the update payload (not left for DB default)
    - cycleLength stepper is bounded: min 21, max 45
    - periodLength stepper is bounded: min 3, max 8
    - date input has max={new Date().toISOString().split('T')[0]} (prevents future dates)
    - "My Cycle" card section is placed between Appearance and Account sections in the JSX
    - When lastPeriodDate is empty, a hint "Add your last period date to unlock cycle-synced targets." is shown
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>My Cycle card in ProfileScreen allows date input, cycle length stepper (21–45), period length stepper (3–8), and save with explicit updated_at update. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Wire ?phase=luteal query param into RecipesScreen</name>
  <files>src/screens/recipes/RecipesScreen.tsx</files>
  <read_first>
    - src/screens/recipes/RecipesScreen.tsx (full current file — understand existing Filter type, FILTERS array, activeFilter state, FILTER_TAG map)
    - .planning/phases/05-cycle-synced-targets/05-CONTEXT.md (D-11 — "?phase=luteal query param that pre-applies a Luteal-friendly filter")
  </read_first>
  <action>
    Add import: import { useSearchParams } from 'react-router-dom'

    Inside RecipesScreen, add useSearchParams:
      const [searchParams] = useSearchParams()

    Add a useEffect that fires once on mount to apply the pre-filter:
      useEffect(() => {
        const phase = searchParams.get('phase')
        if (phase === 'luteal') {
          setActiveFilter('Low GL')
          setShowFavsOnly(false)
        }
      }, [])  // empty deps — run once on mount only

    Note: The empty dependency array is correct here because the phase param is a navigation intent (set by LutealTipCard CTA) and should only apply once when the screen loads, not re-run on every render.

    No other changes to RecipesScreen. The FILTER_TAG map already maps 'Low GL' to 'low-gl', and matchesFilter already handles this. The filter chip for 'Low GL' will appear active. The featured section header already renders the active filter name ("Low GL") when activeFilter !== 'All'.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - RecipesScreen.tsx imports useSearchParams from 'react-router-dom'
    - useSearchParams() is called inside RecipesScreen
    - useEffect reads searchParams.get('phase') and calls setActiveFilter('Low GL') when phase === 'luteal'
    - The useEffect has an empty dependency array (runs once on mount)
    - No existing filter logic, FILTERS array, FILTER_TAG map, or CATEGORIES array is modified
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>RecipesScreen reads ?phase=luteal on mount and pre-selects the Low GL filter. Navigating from LutealTipCard CTA (/recipes?phase=luteal) shows only low-GL recipes. TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → profiles.last_period_date | Date string from a date input; written through RLS-enforced update |
| URL query param → activeFilter state | ?phase=luteal drives a UI-only state change, no DB write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | profiles.update() in handleSaveCycle | mitigate | Supabase client uses user JWT; RLS policy profiles_update_own enforces (select auth.uid()) = id — user can only update their own row |
| T-05-09 | Tampering | ?phase= query param injection | accept | param only controls which Filter chip is pre-selected (UI state); no DB write from query param; any unrecognized value is ignored (only 'luteal' triggers action) |
| T-05-10 | Information Disclosure | cravings data in symptom_logs | mitigate | RLS policy symptom_logs_select_own restricts reads to the row owner; same as existing energy/mood/sleep/bloating/skin columns |
| T-05-11 | Denial of Service | handleSaveCycle without debounce | accept | Save button is disabled while cycleSaving=true; double-submit is prevented at UI level |
| T-05-SC | Tampering | No new npm/pip installs in this plan | accept | No new package manager installs |
</threat_model>

<verification>
After completing all tasks:
1. Run: npx tsc --noEmit (must pass with 0 errors)
2. Run: grep -n "cravings" src/hooks/useUpsertSymptomLog.ts (expect SymptomPayload field and upsert line)
3. Run: grep -n "cravings" src/screens/symptoms/SymptomsScreen.tsx (expect SymValues, EMPTY, VALUE_LABELS, SYMPTOMS array, useEffect)
4. Run: grep -n "last_period_date" src/screens/profile/ProfileScreen.tsx (expect state, useEffect, update payload)
5. Run: grep -n "updated_at" src/screens/profile/ProfileScreen.tsx (expect updated_at: new Date().toISOString() in update payload)
6. Run: grep -n "useSearchParams\|phase.*luteal" src/screens/recipes/RecipesScreen.tsx (expect import and useEffect)
7. Manual: Navigate to /profile, set last period date 21 days ago, cycle 28 days, save → check that targets regenerate (useAITargets fires). Navigate to /home → chip shows "Luteal · Day 21", LutealTipCard visible. Tap CTA → /recipes pre-filtered to Low GL.
</verification>

<success_criteria>
- Cravings symptom: SymptomPayload, SymptomsScreen SymValues, SYMPTOMS array, VALUE_LABELS, useEffect sync, and upsert all include cravings
- My Cycle section in ProfileScreen: date picker (max=today), cycle_length_days stepper (21–45), period_length_days stepper (3–8), save writes updated_at explicitly
- RecipesScreen pre-selects Low GL filter when ?phase=luteal is in the URL
- npx tsc --noEmit exits 0 across all modified files
</success_criteria>

<output>
Create .planning/phases/05-cycle-synced-targets/05-03-SUMMARY.md when done.
</output>
